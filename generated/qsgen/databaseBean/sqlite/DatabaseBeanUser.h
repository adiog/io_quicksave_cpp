// This file is an AUTOGENERATED part of beans project.
// Copyright (c) 2017 Aleksander Gajewski <adiog@quicksave.io>.

#pragma once

#include <iostream>
#include <string>

#include <folly/Format.h>

#include <SQLiteCpp/SQLiteCpp.h>

#include <CppBeans.h>
#include <qs/database/SqliteTransaction.h>
#include <qs/util/hash.h>
#include <qsgen/bean/UserBean.h>

template <typename DB, typename T> class DatabaseBean;

using DB = SQLite::Database;

template <> class DatabaseBean<DB, UserBean> {
public:
  static absl::optional<UserBean> get(DB *sqliteDatabase, std::string hash) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "SELECT * FROM user WHERE user_hash = ?");

      query.bind(1, hash);

      while (query.executeStep()) {
        UserBean bean;

        int getIndex = 0;
        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.databaseConnectionString = query.getColumn(getIndex++).getString();
        bean.password = query.getColumn(getIndex++).getString();
        bean.storageConnectionString = query.getColumn(getIndex++).getString();
        bean.username = query.getColumn(getIndex++).getString();

        return bean;
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return absl::nullopt;
    }
  }

  template <typename FIELD_VALUE>
  static List<UserBean> get_by(DB *sqliteDatabase, std::string field,
                               FIELD_VALUE field_value) {
    List<UserBean> result(0);

    try {
      std::string query_str =
          folly::format("SELECT * FROM user WHERE {} = ?", field.c_str()).str();
      SQLite::Statement query(*sqliteDatabase, query_str.c_str());

      query.bind(1, field_value);

      while (query.executeStep()) {
        UserBean bean;

        int getIndex = 0;
        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.databaseConnectionString = query.getColumn(getIndex++).getString();
        bean.password = query.getColumn(getIndex++).getString();
        bean.storageConnectionString = query.getColumn(getIndex++).getString();
        bean.username = query.getColumn(getIndex++).getString();

        result.push_back(bean);
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return result;
    }

    return result;
  }

  static void remove(DB *sqliteDatabase, std::string hash) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "DELETE FROM user WHERE user_hash = ?");

      query.bind(1, hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  template <typename FIELD_VALUE>
  static void remove_by(DB *sqliteDatabase, std::string field,
                        FIELD_VALUE field_value) {
    try {
      std::string query_str =
          folly::format("DELETE FROM user WHERE {} = ?", field.c_str()).str();
      SQLite::Statement query(*sqliteDatabase, query_str.c_str());

      query.bind(1, field_value);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static List<UserBean> sql(DB *sqliteDatabase, std::string sql) {
    List<UserBean> result(0);

    try {
      SQLite::Statement query(*sqliteDatabase, sql.c_str());

      while (query.executeStep()) {
        UserBean bean;
        int getIndex = 0;

        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.databaseConnectionString = query.getColumn(getIndex++).getString();
        bean.password = query.getColumn(getIndex++).getString();
        bean.storageConnectionString = query.getColumn(getIndex++).getString();
        bean.username = query.getColumn(getIndex++).getString();

        result.push_back(bean);
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return result;
    }

    return result;
  }

  static std::string insert(DB *sqliteDatabase, UserBean bean) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "INSERT INTO user ('user_hash', "
                              "'databaseConnectionString', 'password', "
                              "'storageConnectionString', 'username') VALUES "
                              "(?, ?, ?, ?, ?)");

      int bindIndex = 1;
      if (!bean.user_hash)
        bean.user_hash = qs::util::Hash::get();
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      else
        query.bind(bindIndex++);
      query.bind(bindIndex++, bean.databaseConnectionString);
      query.bind(bindIndex++, bean.password);
      query.bind(bindIndex++, bean.storageConnectionString);
      query.bind(bindIndex++, bean.username);

      query.exec();

      return *bean.user_hash;
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void insert_with_pk(DB *sqliteDatabase, UserBean bean) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "INSERT INTO user ('user_hash', "
                              "'databaseConnectionString', 'password', "
                              "'storageConnectionString', 'username') VALUES "
                              "(?, ?, ?, ?, ?)");

      int bindIndex = 1;
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      else
        query.bind(bindIndex++);
      query.bind(bindIndex++, bean.databaseConnectionString);
      query.bind(bindIndex++, bean.password);
      query.bind(bindIndex++, bean.storageConnectionString);
      query.bind(bindIndex++, bean.username);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void update(DB *sqliteDatabase, UserBean bean) {
    try {
      std::string setBuilder = "";
      setBuilder += ((setBuilder != "") ? std::string(", ") : std::string("")) +
                    std::string("databaseConnectionString = ?");
      setBuilder += ((setBuilder != "") ? std::string(", ") : std::string("")) +
                    std::string("password = ?");
      setBuilder += ((setBuilder != "") ? std::string(", ") : std::string("")) +
                    std::string("storageConnectionString = ?");
      setBuilder += ((setBuilder != "") ? std::string(", ") : std::string("")) +
                    std::string("username = ?");
      std::string query_template =
          folly::format("UPDATE user SET {} WHERE user_hash = ?",
                        setBuilder.c_str())
              .str();
      SQLite::Statement query(*sqliteDatabase, query_template.c_str());

      int bindIndex = 1;
      query.bind(bindIndex++, bean.databaseConnectionString);
      query.bind(bindIndex++, bean.password);
      query.bind(bindIndex++, bean.storageConnectionString);
      query.bind(bindIndex++, bean.username);

      query.bind(bindIndex++, *bean.user_hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void override(DB *sqliteDatabase, UserBean bean) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "UPDATE user SET user_hash = ?, "
                              "databaseConnectionString = ?, password = ?, "
                              "storageConnectionString = ?, username = ? WHERE "
                              "user_hash = ?");

      int bindIndex = 1;

      query.bind(bindIndex++, bean.databaseConnectionString);
      query.bind(bindIndex++, bean.password);
      query.bind(bindIndex++, bean.storageConnectionString);
      query.bind(bindIndex++, bean.username);

      query.bind(bindIndex++, *bean.user_hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }
};
