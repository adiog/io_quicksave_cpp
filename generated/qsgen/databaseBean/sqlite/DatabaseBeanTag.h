// This file is an AUTOGENERATED part of beans project.
// Copyright (c) 2017 Aleksander Gajewski <adiog@quicksave.io>.

#pragma once

#include <iostream>
#include <string>

#include <folly/Format.h>

#include <SQLiteCpp/SQLiteCpp.h>

#include <CppBeans.h>
#include <qs/database/SqliteTransaction.h>
#include <qs/util/hash.h>
#include <qsgen/bean/TagBean.h>

template <typename DB, typename T> class DatabaseBean;

using DB = SQLite::Database;

template <> class DatabaseBean<DB, TagBean> {
public:
  static absl::optional<TagBean> get(DB *sqliteDatabase, std::string hash) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "SELECT * FROM tag WHERE tag_hash = ?");

      query.bind(1, hash);

      while (query.executeStep()) {
        TagBean bean;

        int getIndex = 0;
        bean.tag_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.meta_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.name = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.value = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());

        return bean;
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return absl::nullopt;
    }
  }

  template <typename FIELD_VALUE>
  static List<TagBean> get_by(DB *sqliteDatabase, std::string field,
                              FIELD_VALUE field_value) {
    List<TagBean> result(0);

    try {
      std::string query_str =
          folly::format("SELECT * FROM tag WHERE {} = ?", field.c_str()).str();
      SQLite::Statement query(*sqliteDatabase, query_str.c_str());

      query.bind(1, field_value);

      while (query.executeStep()) {
        TagBean bean;

        int getIndex = 0;
        bean.tag_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.meta_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.name = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.value = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());

        result.push_back(bean);
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return result;
    }

    return result;
  }

  static void remove(DB *sqliteDatabase, std::string hash) {
    try {
      SQLite::Statement query(*sqliteDatabase,
                              "DELETE FROM tag WHERE tag_hash = ?");

      query.bind(1, hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  template <typename FIELD_VALUE>
  static void remove_by(DB *sqliteDatabase, std::string field,
                        FIELD_VALUE field_value) {
    try {
      std::string query_str =
          folly::format("DELETE FROM tag WHERE {} = ?", field.c_str()).str();
      SQLite::Statement query(*sqliteDatabase, query_str.c_str());

      query.bind(1, field_value);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static List<TagBean> sql(DB *sqliteDatabase, std::string sql) {
    List<TagBean> result(0);

    try {
      SQLite::Statement query(*sqliteDatabase, sql.c_str());

      while (query.executeStep()) {
        TagBean bean;
        int getIndex = 0;

        bean.tag_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.meta_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.name = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.user_hash = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());
        bean.value = absl::make_optional<std::string>(
            query.getColumn(getIndex++).getString());

        result.push_back(bean);
      }
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
      return result;
    }

    return result;
  }

  static std::string insert(DB *sqliteDatabase, TagBean bean) {
    try {
      SQLite::Statement query(
          *sqliteDatabase, "INSERT INTO tag ('tag_hash', 'meta_hash', 'name', "
                           "'user_hash', 'value') VALUES (?, ?, ?, ?, ?)");

      int bindIndex = 1;
      if (!bean.tag_hash)
        bean.tag_hash = qs::util::Hash::get();
      if (bean.tag_hash)
        query.bind(bindIndex++, *bean.tag_hash);
      else
        query.bind(bindIndex++);
      if (bean.meta_hash)
        query.bind(bindIndex++, *bean.meta_hash);
      else
        query.bind(bindIndex++);
      if (bean.name)
        query.bind(bindIndex++, *bean.name);
      else
        query.bind(bindIndex++);
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      else
        query.bind(bindIndex++);
      if (bean.value)
        query.bind(bindIndex++, *bean.value);
      else
        query.bind(bindIndex++);

      query.exec();

      return *bean.tag_hash;
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void insert_with_pk(DB *sqliteDatabase, TagBean bean) {
    try {
      SQLite::Statement query(
          *sqliteDatabase, "INSERT INTO tag ('tag_hash', 'meta_hash', 'name', "
                           "'user_hash', 'value') VALUES (?, ?, ?, ?, ?)");

      int bindIndex = 1;
      if (bean.tag_hash)
        query.bind(bindIndex++, *bean.tag_hash);
      else
        query.bind(bindIndex++);
      if (bean.meta_hash)
        query.bind(bindIndex++, *bean.meta_hash);
      else
        query.bind(bindIndex++);
      if (bean.name)
        query.bind(bindIndex++, *bean.name);
      else
        query.bind(bindIndex++);
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      else
        query.bind(bindIndex++);
      if (bean.value)
        query.bind(bindIndex++, *bean.value);
      else
        query.bind(bindIndex++);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void update(DB *sqliteDatabase, TagBean bean) {
    try {
      std::string setBuilder = "";
      if (bean.meta_hash) {
        setBuilder +=
            ((setBuilder != "") ? std::string(", ") : std::string("")) +
            std::string("meta_hash = ?");
      }
      if (bean.name) {
        setBuilder +=
            ((setBuilder != "") ? std::string(", ") : std::string("")) +
            std::string("name = ?");
      }
      if (bean.user_hash) {
        setBuilder +=
            ((setBuilder != "") ? std::string(", ") : std::string("")) +
            std::string("user_hash = ?");
      }
      if (bean.value) {
        setBuilder +=
            ((setBuilder != "") ? std::string(", ") : std::string("")) +
            std::string("value = ?");
      }
      std::string query_template =
          folly::format("UPDATE tag SET {} WHERE tag_hash = ?",
                        setBuilder.c_str())
              .str();
      SQLite::Statement query(*sqliteDatabase, query_template.c_str());

      int bindIndex = 1;
      if (bean.meta_hash)
        query.bind(bindIndex++, *bean.meta_hash);
      if (bean.name)
        query.bind(bindIndex++, *bean.name);
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      if (bean.value)
        query.bind(bindIndex++, *bean.value);

      query.bind(bindIndex++, *bean.tag_hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }

  static void override(DB *sqliteDatabase, TagBean bean) {
    try {
      SQLite::Statement query(
          *sqliteDatabase, "UPDATE tag SET tag_hash = ?, meta_hash = ?, name = "
                           "?, user_hash = ?, value = ? WHERE tag_hash = ?");

      int bindIndex = 1;

      if (bean.meta_hash)
        query.bind(bindIndex++, *bean.meta_hash);
      else
        query.bind(bindIndex++);
      if (bean.name)
        query.bind(bindIndex++, *bean.name);
      else
        query.bind(bindIndex++);
      if (bean.user_hash)
        query.bind(bindIndex++, *bean.user_hash);
      else
        query.bind(bindIndex++);
      if (bean.value)
        query.bind(bindIndex++, *bean.value);
      else
        query.bind(bindIndex++);

      query.bind(bindIndex++, *bean.tag_hash);

      query.exec();
    } catch (std::exception &e) {
      std::cout << "exception: " << e.what() << std::endl;
    }
  }
};
